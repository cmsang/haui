<Window x:Class="Haui.PCB.Views.Windows.FiducialTemplateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thư viện mẫu lỗ tròn định vị"
        Height="650" Width="1000"
        MinHeight="480" MinWidth="700"
        WindowStartupLocation="CenterOwner"
        Closed="Window_Closed"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Window.Resources>
        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#106EBE"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC"/>
                                <Setter Property="Foreground" Value="#888888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SecondaryBtn" TargetType="Button">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style x:Key="DangerBtn" TargetType="Button">
            <Setter Property="Background" Value="#D13438"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#A4262C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="260"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="#1E1E1E"
                    Margin="0,0,6,0">
                <Grid>
                    <Image x:Name="SourceImage"
                           Stretch="Uniform"
                           RenderOptions.BitmapScalingMode="LowQuality"/>
                    <TextBlock x:Name="ImagePlaceholder"
                               Text="Đang tải ảnh..."
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Foreground="#888888"
                               FontSize="14"/>
                    <Canvas x:Name="RegionCanvas"
                            Background="Transparent"
                            MouseLeftButtonDown="RegionCanvas_MouseLeftButtonDown"
                            MouseMove="RegionCanvas_MouseMove"
                            MouseLeftButtonUp="RegionCanvas_MouseLeftButtonUp">
                        <Rectangle x:Name="DragRect"
                                   Stroke="Yellow"
                                   StrokeThickness="2"
                                   StrokeDashArray="4 2"
                                   Fill="#330078D4"
                                   Visibility="Collapsed"/>
                    </Canvas>
                </Grid>
            </Border>

            <Border Grid.Column="1"
                    Background="#FAFAFA"
                    BorderBrush="#DDDDDD"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="120"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="Mẫu lỗ tròn"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8"/>
                    <TextBlock Grid.Row="1"
                               Text="Kéo thả nhiều vùng quanh lỗ tròn trên ảnh Morphology Close, rồi bấm Lưu mẫu một lần."
                               TextWrapping="Wrap"
                               Foreground="#666666"
                               FontSize="12"
                               Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="2"
                               Text="Vùng chờ lưu"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Margin="0,0,0,6"/>
                    <ListBox Grid.Row="3"
                             x:Name="PendingRegionsList"
                             SelectionChanged="PendingRegionsList_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                               Text="{Binding Label}"
                                               VerticalAlignment="Center"/>
                                    <Button Grid.Column="1"
                                            Content="Xóa"
                                            Style="{StaticResource DangerBtn}"
                                            Click="BtnRemovePendingRegion_Click"
                                            Tag="{Binding}"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <Button Grid.Row="4"
                            x:Name="BtnSave"
                            Content="💾 Lưu mẫu"
                            Height="34"
                            Margin="0,10,0,8"
                            Click="BtnSave_Click"
                            Style="{StaticResource PrimaryBtn}"/>

                    <Button Grid.Row="5"
                            x:Name="BtnClearSelection"
                            Content="Xóa tất cả vùng"
                            Height="30"
                            Margin="0,0,0,12"
                            Click="BtnClearSelection_Click"
                            Style="{StaticResource SecondaryBtn}"/>

                    <TextBlock Grid.Row="6"
                               Text="Thư viện mẫu đã lưu"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Margin="0,0,0,6"/>
                    <ListBox Grid.Row="7"
                             x:Name="SavedTemplatesList"
                             VerticalAlignment="Stretch">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                               Text="{Binding}"
                                               VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis"/>
                                    <Button Grid.Column="1"
                                            Content="Xóa"
                                            Style="{StaticResource DangerBtn}"
                                            Click="BtnDeleteTemplate_Click"
                                            Tag="{Binding}"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="1"
                Background="#F3F3F3"
                BorderBrush="#DDDDDD"
                BorderThickness="0,1,0,0"
                Padding="10,6">
            <TextBlock x:Name="StatusText"
                       Foreground="#555555"
                       FontSize="12"/>
        </Border>
    </Grid>
</Window>
