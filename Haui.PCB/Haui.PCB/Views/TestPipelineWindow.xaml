<Window x:Class="Haui.PCB.Views.TestPipelineWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Test Pipeline - PCB Segmentation"
        Height="600" Width="1000"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">

    <Window.Resources>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0078D4" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#106EBE" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC" />
                                <Setter Property="Foreground" Value="#888888" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F0F0F0" />
            <Setter Property="Foreground" Value="#333333" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#CCCCCC" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E0E0E0" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#F5F5F5" />
                                <Setter Property="Foreground" Value="#AAAAAA" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Khu vực hiển thị 2 ảnh -->
        <Grid Grid.Row="0" Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Ảnh gốc -->
            <Border Grid.Column="0"
                    BorderBrush="#CCCCCC" BorderThickness="1"
                    Background="#F5F5F5">
                <Grid>
                    <TextBlock Text="Ảnh gốc"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Top"
                               Margin="0,6,0,0"
                               FontWeight="SemiBold"
                               Foreground="#555555" />
                    <Image x:Name="OriginalImage"
                           Margin="0,28,0,0"
                           Stretch="Uniform"
                           RenderOptions.BitmapScalingMode="HighQuality" />
                    <TextBlock x:Name="OriginalPlaceholder"
                               Text="Chưa có ảnh"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Foreground="#AAAAAA"
                               FontSize="14" />
                </Grid>
            </Border>

            <!-- Ảnh đã xử lý -->
            <Border Grid.Column="2"
                    BorderBrush="#CCCCCC" BorderThickness="1"
                    Background="#F5F5F5">
                <Grid>
                    <TextBlock Text="Ảnh sau xử lý (PCB đã cắt)"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Top"
                               Margin="0,6,0,0"
                               FontWeight="SemiBold"
                               Foreground="#555555" />
                    <Image x:Name="ProcessedImage"
                           Margin="0,28,0,0"
                           Stretch="Uniform"
                           RenderOptions.BitmapScalingMode="HighQuality" />
                    <TextBlock x:Name="ProcessedPlaceholder"
                               Text="Chưa xử lý"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Foreground="#AAAAAA"
                               FontSize="14" />
                </Grid>
            </Border>
        </Grid>

        <!-- Thanh nút phía dưới -->
        <Border Grid.Row="1"
                BorderBrush="#DDDDDD" BorderThickness="0,1,0,0"
                Padding="12,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="BtnSelectImage"
                        Content="🖼 Chọn ảnh"
                        Width="130" Height="36"
                        Margin="0,0,12,0"
                        Click="BtnSelectImage_Click"
                        Style="{StaticResource SecondaryButtonStyle}" />
                <Button x:Name="BtnTest"
                        Content="▶ Test"
                        Width="130" Height="36"
                        IsEnabled="False"
                        Click="BtnTest_Click"
                        Style="{StaticResource PrimaryButtonStyle}" />
                <TextBlock x:Name="StatusText"
                           VerticalAlignment="Center"
                           Margin="16,0,0,0"
                           Foreground="#666666"
                           FontSize="12" />
            </StackPanel>
        </Border>
    </Grid>
</Window>